import React from "react";

import Layout from "../template/Layout";

const HomePage = () => {
    return (
        <Layout>
            Home Page
        </Layout>
    );
};

export default HomePage;
